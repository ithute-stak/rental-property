services:
  migrate:
    image: ${API_IMAGE:?API_IMAGE must reference the immutable Mosala backend image}
    pull_policy: always

  api:
    image: ${API_IMAGE:?API_IMAGE must reference the immutable Mosala backend image}
    pull_policy: always

  worker:
    image: ${API_IMAGE:?API_IMAGE must reference the immutable Mosala backend image}
    pull_policy: always
